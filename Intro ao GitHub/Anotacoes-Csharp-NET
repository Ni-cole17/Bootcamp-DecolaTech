# Estruturas
 - Namespaces
 - Classes
 - Interface
 - Enum
 
 ## Namespaces 
  Utilizado para organizar as classes, podemos usar ele quantas vezes precisar. Quando queremos chamar um classe que está dentro de um Namespace, primeiro colocamos o namespace seguido de um ponto seguido do nome da classe.
  [Nome do namespace].[nome da classe]
  Podemos ter duas classes com nomes iguais porém em namespaces diferentes
  
  ## Classes
  Uma classe tem campos, propriedades, métodos e eventos que são denominados membros de uma classe.
  Se uma classe é instanciada, essa instância é chamada de Objeto.
  Podemos utilizar classes da seguinte forma, criando a quantidade de instâncias que precisamos:
  var [nome da instância] = new [nome da classe]();
  [nome da instância].[variáveis da classe]
  
  ## Interface
  Serve como um molde para várias classes, importante em certos casos que é preciso preencher ou completar todos os métodos de uma classe.
  
  ## Enum
  Declara um conjunto de constantes numeradas que vão do zero e aumentam de um em um.
